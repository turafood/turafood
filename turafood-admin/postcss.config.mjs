/**
 * Sin Tailwind a propósito.
 *
 * El resto de TuraFood usa variables CSS y estilos en línea; meter
 * Tailwind solo aquí haría que el panel se viera parecido pero se
 * mantuviera distinto. globals.css es el mismo archivo que la app de
 * negocios, copiado tal cual.
 */
export default { plugins: {} };
